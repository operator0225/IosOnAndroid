[package]
name = "iosonandroid-metal-vulkan"
description = "Metal-inspired resource/pipeline descriptors translated to and executed via real Vulkan (ash)"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "metal_vulkan"
path = "src/lib.rs"

[dependencies]
ash = "0.38"
