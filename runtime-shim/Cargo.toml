[package]
name = "iosonandroid-runtime-shim"
description = "dyld-equivalent symbol resolution and Darwin threading-primitive shims"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "runtime_shim"
path = "src/lib.rs"

[dependencies]
iosonandroid-loader = { path = "../loader" }
libc = "0.2"
