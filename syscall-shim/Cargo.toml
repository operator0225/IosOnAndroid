[package]
name = "iosonandroid-syscall-shim"
description = "Darwin/XNU syscall table and translation to Linux syscalls"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "syscall_shim"
path = "src/lib.rs"

[target.'cfg(any(target_os = "linux", target_os = "android"))'.dependencies]
libc = "0.2"
